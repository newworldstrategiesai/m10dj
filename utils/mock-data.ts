import type { Email, EmailAccount } from "@/types/email"

// Mock data no longer used - email client now uses real API data from Resend webhooks
export const mockAccounts: EmailAccount[] = []

export const mockEmails: Email[] = []
